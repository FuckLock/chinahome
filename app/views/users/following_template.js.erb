var following = $('<%= j render partial: "following_list", collection: @users, as: :user, locals: { sidebar_user: @user } %>')
$('.following-list-row').html(following)
var followInfo = $('<%= j render "follow_info", user: @user %>')
$(".follow-info").html(followInfo)
