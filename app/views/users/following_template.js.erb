var following = $('<%= j render "following_list", users: @users, user: @user %>')
$('#menu-bottom').html(following)
var followInfo = $('<%= j render "follow_info", user: @user %>')
$(".follow-info").html(followInfo)
