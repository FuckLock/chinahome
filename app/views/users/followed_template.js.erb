var followed = $('<%= j render partial: "followed_list", collection: @users, as: :user, locals: { sidebar_user: @user} %>')
$('.followed-list-row').html(followed)
var followInfo = $('<%= j render "follow_info", user: @user %>')
$(".follow-info").html(followInfo)
